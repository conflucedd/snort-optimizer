这是snort性能优化器，wrap里是snort的go wrap，和本次任务关系不大，analyser里是rule性能分析器，作用是根据rule的性能分析，误报率，未检出率等进行裁剪。
目前你的主要任务是写analyser。analyser文件夹下会有snort_output.txt(包含警报和性能分析数据)，Friday-WorkingHours.csv（给定的数据集中流的标签，根据这个文件计算检出率和未检出率），注意Friday-WorkingHours.csv的timestamp有时精确到分钟，有时精确到秒。
步骤
1. 根据snort_output.txt，使用sqlite建立2个表以便后续分析
 （1）警报的表：包含警报中的规则；警报中包对应的流的source ip，source port，target ip，target port，协议；误报标记；未检出标记。注意，snort以packet为单位而非流，确定警报中的包属于哪个流的方法如下：对于一个警报的packet，在表中能找到相同的source ip，source port，target ip，target port，协议，且时间戳差异小于2min，则记为同一个流。若表中无法找出，则记为新流，流的timestamp即为首包timestamp，流和规则都相同的警报只记一次。
 （2）规则的profiler表（性能分析表）
2. 读取Friday-WorkingHours.csv并查数据库，若其中流的标签是BENIGN但被误报，则把误报标记置为1，若流的标签非BENIGN但未检出，则在数据库中增加一条这条流的记录。查表时需要source ip，source port，target ip，target port，协议5个值相同，警报的表中的流时间戳只要在csv中的流开始时间戳~流开始时间戳+流持续时间（csv中，以微秒为单位）+1min即算同一条流。
3. 根据这个数据库计算误报率和未检出率
4. 根据以下参数裁剪（要做到参数可开关，可调整，有默认）
 （1）误报率
 （2）未检出率
 （3）性能分析中的数据（比如触发检查的次数，最终匹配成功的次数，耗时，平均每次未命中耗时等等）
 （3）snort跑在服务器上，所以可以根据systemctl status，或者查看服务器被占用的端口看启用的服务（只支持常用的https，ssh等即可），然后裁剪不需要的规则
 （4）两条规则的报警很重合，就可以只保留一条
 （5）若是服务器而不是网关，可以裁剪全部的应用识别（AppID）相关的检测规则
5. 最后以json数组输出可被裁剪的规则id。


你正在为一个 Snort 性能优化器项目编写 analyser 模块。项目中 wrap 是 Snort 的 Go wrapper，与本任务关系不大。你的主要任务是实现 analyser：读取 Snort 输出与 CIC 流量标签 CSV，建立 SQLite 数据库，计算误报/未检出，并输出可裁剪规则列表。

## 技术要求
- 使用 Go 实现
- analyser 目录内开发
- 优先实现最小可用版本，不要过度设计，不要引入复杂框架
- 数据库存储使用 SQLite
- 代码应可直接通过命令行运行
- 先保证核心逻辑正确，再考虑扩展功能
- 需要写清晰注释，尤其是时间匹配、流归并、误报/漏报判定逻辑

## 输入文件
analyser 目录下有：
1. `snort_output.txt`
   - 包含 Snort 警报输出
   - 也包含 profiler/perf 相关性能分析数据
2. `Friday-WorkingHours.csv`
   - 数据集中每条流的标签
   - 用于计算误报率和未检出率
   - 注意：timestamp 有时精确到分钟，有时精确到秒

## 目标
实现一个 analyser 程序，完成以下步骤：

## 步骤 1：建立 SQLite 数据库
根据 `snort_output.txt` 建立 SQLite 数据库，至少包含 2 张表：

### 1. alerts 表
用于存储警报归并后的“规则-流”记录，至少包含这些字段：
- id
- rule_gid
- rule_sid
- rule_rev
- rule_text（如果无法从输入中提取可留空）
- src_ip
- src_port
- dst_ip
- dst_port
- protocol
- flow_timestamp
- first_alert_timestamp
- false_positive（0/1）
- missed_detection（0/1）
- alert_count
- source = 'snort'

说明：
- Snort 是按 packet 告警，不是按 flow 告警
- 对于一个警报 packet，若数据库中已存在一条记录满足：
  - src_ip 相同
  - src_port 相同
  - dst_ip 相同
  - dst_port 相同
  - protocol 相同
  - 时间戳差异 < 2 分钟
  - rule_gid/rule_sid/rule_rev 相同
  则视为同一条“规则命中的流”，不新增记录，只更新计数
- 若找不到，则新增一条记录
- 新增记录时，`flow_timestamp` 取该流首次出现的包时间
- 同一条流、同一条规则，只保留一条 alerts 记录

### 2. rule_profiler 表
用于存储 profiler/perf 性能分析结果，字段可根据 `snort_output.txt` 中实际可解析内容设计，但至少尽量包含：
- id
- rule_gid
- rule_sid
- rule_rev
- checks
- matches
- no_matches
- total_ticks_or_time
- avg_match_time
- avg_no_match_time
- raw_line

要求：
- 优先保证能把 profiler 中每条规则的关键性能指标结构化入库
- 若某些字段在原始文本中无法稳定提取，可保留 raw_line 以便后续分析

## 步骤 2：读取 CSV 并回填误报/未检出
读取 `Friday-WorkingHours.csv`，对每条流做匹配。

CSV 中至少会使用这些字段：
- Source IP
- Source Port
- Destination IP
- Destination Port
- Protocol
- Timestamp
- Flow Duration
- Label

注意：
- `Flow Duration` 单位是微秒
- `Protocol` 可能是数字，需要与 Snort 的协议字符串做映射，例如：
  - 6 -> TCP
  - 17 -> UDP
  - 1 -> ICMP
- `Timestamp` 可能有两种精度：到分钟，或到秒，需要兼容解析

### 流匹配规则
用以下条件判断 CSV 中一条流是否对应 alerts 表中的一条流：
- src_ip 相同
- src_port 相同
- dst_ip 相同
- dst_port 相同
- protocol 相同
- 且 alerts.flow_timestamp 落在以下区间内：
  - [csv_timestamp, csv_timestamp + flow_duration + 1分钟]

### 回填逻辑
1. 若 CSV 的 `Label == BENIGN`
   - 且数据库中找到了匹配 alerts 记录
   - 则将对应 alerts 记录的 `false_positive = 1`

2. 若 CSV 的 `Label != BENIGN`
   - 且数据库中没有找到任何匹配 alerts 记录
   - 则表示该恶意流未被检出
   - 需要在 alerts 表中新增一条记录，字段如下：
     - rule_gid/rule_sid/rule_rev 置空或为 NULL
     - src/dst/protocol/flow_timestamp 从 CSV 填充
     - false_positive = 0
     - missed_detection = 1
     - source = 'csv_missed'

请不要为同一条 CSV 恶意流重复插入多条 missed_detection 记录。

## 步骤 3：计算指标
基于数据库计算：
1. 整体误报率
2. 整体未检出率
3. 每条规则的误报统计
4. 每条规则的命中数量
5. 每条规则的性能统计（结合 rule_profiler）

请在代码中明确误报率和未检出率的计算公式，并写在注释里。
默认按“流”粒度统计，而不是按 packet。

## 步骤 4：根据规则筛选可裁剪项
根据以下可配置参数筛选“可被裁剪的规则”，要求所有参数都支持：
- 开关
- 默认值
- 命令行调整

至少支持这些筛选条件：

### 4.1 高误报率规则
若某条规则误报率超过阈值，则标记为候选裁剪

### 4.2 高未检出关联规则
若某类恶意流长期未被相关规则覆盖，可作为辅助分析信息
注意：如果一条流未检出，通常不能直接推出某条具体规则应该删除，因此这里需要谨慎实现，不要做错误归因。
如果无法可靠归因，就只把它作为统计信息，不直接参与裁剪决定。

### 4.3 性能差但收益低的规则
结合 profiler 中的数据，例如：
- 检查次数很多
- 最终匹配次数很少
- 总耗时高
- 平均未命中耗时高
可作为候选裁剪规则

### 4.4 服务无关规则
Snort 跑在服务器上，不是网关。程序可以通过以下方式推断服务器启用的服务：
- `systemctl status`
- 或检查监听端口
只需要支持常见服务识别即可，例如：
- HTTP/HTTPS
- SSH
- DNS
- SMTP
- MySQL/PostgreSQL
如果某些规则明显针对服务器未启用的服务，可标记为候选裁剪。
这一部分先做成可选增强功能，失败时不要影响主流程。

### 4.5 高重合规则
如果两条规则命中的流高度重合，则只保留一条。
这里“高度重合”请实现为可配置阈值，例如：
- overlap_ratio >= 0.9
并在代码中明确重合度公式。
优先保留：
- 命中更多的规则
- 或性能更好的规则
- 或 SID 更稳定/更常见的规则（写清策略）

### 4.6 AppID 相关规则
如果运行场景是服务器而不是网关，则支持将全部 AppID 相关规则标记为候选裁剪。
这一判断应做成可配置开关，不要写死。

## 步骤 5：输出结果
最终输出 JSON 数组，内容为“可被裁剪的规则”，每项至少包含：
- gid
- sid
- rev
- reasons（数组，说明为什么建议裁剪）
- metrics（包含误报率、命中次数、性能指标、重合度等可用信息）

示例格式：
[
  {
    "gid": 1,
    "sid": 12345,
    "rev": 3,
    "reasons": ["high_false_positive", "low_value_high_cost"],
    "metrics": {
      "false_positive_rate": 0.92,
      "hit_count": 3,
      "check_count": 100000,
      "match_count": 3,
      "avg_no_match_time": 12.5
    }
  }
]

## 命令行要求
请实现一个 CLI，例如：
`go run ./analyser --snort snort_output.txt --csv Friday-WorkingHours.csv --db analyser.db`

支持可选参数，例如：
- `--fp-threshold`
- `--overlap-threshold`
- `--enable-service-prune`
- `--enable-appid-prune`
- `--enable-overlap-prune`
- `--enable-profiler-prune`

## 实现注意事项
- 先实现数据库建表、Snort 解析、CSV 匹配、JSON 输出这条主链路
- 对时间解析写独立函数，兼容多种时间格式
- 对协议映射写独立函数
- 对流匹配逻辑写清晰注释
- 遇到无法解析的行，不要直接崩溃，应记录 warning 并继续
- 如果某些 profiler 字段无法从样本稳定解析，先把原始行存库并尽量提取 gid/sid/rev
- 不要假设输入 100% 干净

## 开发顺序
请按以下顺序实现：
1. 设计 SQLite schema
2. 实现 `snort_output.txt` 的 alerts 解析与入库
3. 实现 profiler 解析与入库
4. 实现 CSV 读取与误报/未检出回填
5. 实现统计计算
6. 实现裁剪规则筛选
7. 实现 JSON 输出
8. 最后整理代码结构

先给出：
1. 你设计的表结构
2. 主要 Go 文件划分
3. 然后开始写代码