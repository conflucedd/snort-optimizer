你的任务是把wrap和analyser结合起来，现在analyser还只是一个独立的cmd程序，你要让wrap能调用analyser module并在http中提供服务。
analyser的说明是：

wrap调用analyser的要求是：
配置文件

wrap的任务增加：
启动时创建配置库，给出默认，然后按需禁用。
wizard/binder优化
系统体验优化：硬件offload，cpu亲和性，多线程，抓包方式。

wrap需要提供的服务有：
cpu
内存
查看服务进度（/proc和analyser输出）
配置文件优化：引入snort_optimiser.lua
警告（改成启动时清理，然后tail alert.json）

可以自由重写原来的服务。

wrap文件夹下现在还是很多分散的go文件，请把它们组织成go package，比如把api.go拆分到api文件夹，方便理解。

请在web文件夹写最简单的仅供测试后端的前端页面，不需要css等格式优化。并且把设计好的api写入到docs文件夹，简单叙述，给出发送和返回的json即可。