wrap2/docs/README.md
请实现性能分析器，要求支持cmd和module调用，cmd逻辑写到analyser2/cmd文件夹
传入一个Config，然后根据config初始化Analyser
config包括：
	AnalyserWorkingDir：以下简称awd，分析器的工作目录。
	Mode：工作模式：监督，半监督
		监督模式：有pcap和csv（标记流的好坏），要支持多pcap输入
		半监督模式：有多个参考pcap和pcap，还有实际流量的（可能是多个）real_without_answer.pcap,支持选择real_without_answer.pcap所占的性能优化比例：0就是不优化性能，100%就是不考虑参考文件的性能。
	MaxRound：优化的最大轮数。
	Wrap2Config：用于wrap2的配置
分析方式：
	使用wrap2包提供的go wrap以运行snort，文档在wrap2/docs/README.md
步骤
	0.进行init，处理时区问题等？？？
	1.运行snort，统计cpu时间等性能数据，并落库analyser.db（在awd中，每次运行都要重新初始化），得到RAW_TIME
	2.运行snort，临时禁用所有规则（注意后面还要恢复，建议使用--lua而不是直接修改数据库），运行，统计BASE_TIME
	3.开始执行规则裁剪
		（1）工程裁剪：裁剪明确不需要的规则：比如服务器没运行smtp，那就都裁掉
			得到INIT_OPTIMIZE_TIME
		（2）算法裁剪：多round，每round都裁剪一些，每轮都要把性能统计，误报率，missing率等等落库

返回一个channel，运行时不停的返回一些进度信息。

整体规划：
	不要都放到一个go文件里
	调用Trim（）时先执行一次Optimize_type_a()（工程优化）
	然后再执行Optimize_type_b()