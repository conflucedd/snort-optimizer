在analyser里实现规则分析裁剪性能优化。

Analyser的input：
    pcap1+db（已知内容的实验流量）用于测试误报率和漏报率，样例在data/Tuesday.pcap, data/Tuesday.db
    pcap2（真实业务流量）用于测试性能，样例是data/Monday.db
    AnayserWorkingDir：analyser的工作目录
    snortConfig：snort配置文件位置
    --lua覆写配置，用于精确还原生产环境的覆写
    原始snort.sqlite：用于生成最初的规则配置
    max_round。默认为8
Output：
    需要裁剪的规则和每条规则对应的原因

说明：pcap1和pcap2对应Run()时的两个实例。ExpSnort和RealSnort。Run()要依次串行启动这个实例。还要串行启动一个空pcap实例运行得到加载时间。每次启动都从run_id为0开始，生成run_id为1的规则，再以run_id为1运行，以此类推。SafeFunction指肯定安全的裁剪，iterFunction指需要裁剪然后看效果的函数，可能需要多次运行或者回退。

逻辑：
    在analyser里
    Gen_3_dbs_from_raw_snort.sqlite;（只获取raw db里的rules表中run id为0的rule即可）
    以runid为0 Run() 得到最原始信息 // 使用wrap包

    Register(SafeFunction1, SAFE);
    Register(SafeFunction2, SAFE);
    Register(IterFunction1, ITER);

    Scheduler(factor = 0.8) {
        exec_all_safe_trim_function.

        for i in 1..round_max {
            exec_all_iter_trim_function(round, factor).
            Run() 执行snort并得到这个round的统计结果

            res = Evaluate() 
            if (res very good) { // 漏报率下降不多
                Commit(); //在analyserWorkingDir生成一个analyser.db
            }
            else {
                RollBack();
                factor = factor / 2.
            }
        }
    }

    return all rules with reason。

策略函数的输入：
    三个实例的sqlite.db，用于查询性能信息。
    round，当前round，用于获取当前的round信息。
    factor 裁剪烈度，0-1，1最强。由每个函数自己解释

策略函数的输出：
    裁剪的规则
    函数自带的裁剪原因

package安排：
    cmd
    ExpSnort(pcap1)
    RealSnort(pcap2)
    BaseSnort(空pcap)
    scheduler 调度器
    safe 安全裁剪函数
    iter 迭代裁剪函数
    analyser.go


先生成最简单的两个规则，可以根据tmp/config/rules文件夹生成
安全规则：裁剪所有source file和浏览器相关的规则
iter规则：裁剪性能损耗大的规则

有什么不明白的，或者有更好的建议，请暂停问我。