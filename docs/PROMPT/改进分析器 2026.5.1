改进analyser package。
1.输出帮助的时候要使用“--”而不是“-”
2.去掉那些alias的选项，只保留一个即可
3.警报与流的对应逻辑：根据数据库中流的时间戳和流的持续时间（微秒）计算而不是自定义时间窗口。一个警报对应一条流，注意：无论警报中的方向相同或相反，都算作是警报了这条流，即不看方向。流如果收到多次警报只算一次。以此来计算总体的FP，FN。
4.重新安排analyser的package结构。我的要求是未来所有的safe函数都放到一个名称是safe的文件夹，iter类似，而不是现在这种都散落到package根目录。调度器也要单独开一个scheduler文件夹。很多文件太大了，请你拆成小文件然后放到文件夹里。也不要在根目录里有太多go文件。数据库相关的放到analyser下的sql文件夹。
5.主逻辑在exp的snort实例每轮运行结束后要根据pcap对应的db和alert表计算每条规则的利用率并存放在snort.sqlite中的rule_FP表中（要自己新建）， 注意run_id。

有什么不明白的或者更好的建议可以暂停问我。