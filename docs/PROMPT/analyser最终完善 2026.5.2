对analyser package做最终的完善。原有内容可见docs/analyser
1. 增加策略
    Safe： 
        （1）不常用协议（以tmp/config/rules作为示例的裁剪目标，因为这是面向服务器的，某些面向网关的协议可以直接禁用）比如已经实现的source_file_browser.go
        （2）未启用的服务对应的规则（可以通过systemd status读取，只实现常用的即可）
        （3）孤立flowbits依赖，比如没有规则提供了这个规则所需的flowbit
    Iter：
        （1）报警重合度高的协议，只留覆盖最大的部分。
        （2）高误报，而且检出率低的协议（指真正对应的恶意流很少）。
        （3）被多次调用检查但是检出率低
        （4）单纯性能消耗大（已有）

2. 改进策略函数注册配置，不在New()里默认register，而是在cmd或者调用的analyser的package里注册。cmd提供选项作为策略函数的开关。

3.改进主调度逻辑：default max round现在改为4。
    跑完safe后不再是跑max round轮全部的iter函数。而是先跑max round轮iter1，然后max round轮iter2，以此类推。每次的漏报率下降和以上轮run结果相比进行判断。
    另外不再提供FP表，这个由需要的策略函数根据数据库自己算。

4. 有什么不明白的或者更好的建议请暂停问我。为了粒度更细，某些策略函数可以拆分和合并，这样就允许分别开关。