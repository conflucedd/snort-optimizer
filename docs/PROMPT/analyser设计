工程策略函数可以获得的：
	每个函数自己的Config，规则数据库
	
迭代策略函数可以获得的：
	每个函数自己的Config，规则数据库	
	实验流量：警告数据库，规则数据库，snort规则性能分析数据库，流量分析csv数据库。

返回的：
	被裁剪的规则数组和理由string（策略自带一段理由）

evaluator作为主调度器。调度这两种策略。

evaluator逻辑：
	先执行每个工程策略函数
	再进入round流程
		*.Analyse()
		*.Run()
		*.Evaluate()
		*.Commit()/*Rollback()

	最后直接结束，因为直接修改传入的数据库。