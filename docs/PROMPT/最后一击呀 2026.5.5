对实现的后端（server）和前端（web）进行改进
1.概览界面：
    （1）改进cpu和内存的概览图，纵轴数字坐标固定在左边，cpu底部固定为0，顶部固定为100。mem底部固定为0，顶部可以自由浮动。图没有占满整个框，底部留了空白，需要改进。（但是折线不要一开始就占满，要从右往左移，参考windows10任务管理器）
    （2）抓包只显示相对路径即可，不用重复显示文件名。允许手动停止抓包。
    （3）性能测试开始后，固定配置复选框（不然不知道用哪个配置测试的），允许手动停止。调用wrap的stop即可，会自动生成性能数据）。只有在“使用当前网卡”选项时才显示“时长”，否则隐藏。性能测试结果要显示吞吐率。

2.警告界面：
    查询直接查询所有字段即可。

3.规则优化界面：
    （1）生成的图表，里面的线如果都是0，就和图表底端边框有点重合了，请稍微留一点点空白。
    （2）去掉高误报规则模块，“提交规则”和“规则开关”允许翻页。
    （3）标签db也要用复选框。    
有不明白的请暂停问我。