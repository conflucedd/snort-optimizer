请在 `wrap2` 目录下实现一个更纯粹的 Snort wrap module，供 module 调用和 CLI 调用共用。

项目背景：

* `wrap`：现有 Snort Go wrap + 后端实现
* `analyser`：根据 Snort profiler 结果裁剪规则
* `web`：前端
* 目标：参考 `wrap` 但不要照抄，重新在 `wrap2` 下实现一个职责更单一、结构更清晰的 wrap module

总体要求：

1. 支持 module 调用和 cmd 调用
2. CLI 相关逻辑和主函数放在 `wrap2/cmd`
3. 通用类型放在 `types` 包中
4. 不要把大量逻辑堆在单个 go 文件里，合理拆 package
5. 仅需支持 Linux
6. 不需要 Web API，不需要前端，不需要复杂守护进程
7. 优先使用标准库；不要引入 ORM；SQLite 直接写 SQL

必须输出：
* `types/...`
* `wrap2/cmd/...`
* `wrap2/docs/...`
* `wrap2/types/...`
* `wrap2/runner/...`
* `wrap2/rules/...`
* `wrap2/alerts/...`
* 其他你认为必要的 package
* 完成后请给出目录树

一、配置结构
请定义明确的 `Config` 结构，至少包含：

* Snort 运行模式：监听网卡 / 读取 pcap
* `SWD`：Snort 工作目录
* Snort 配置文件路径
* `LuaOverrides []string`：用于通过 `--lua` 覆写 Snort 配置
* 建议显式包含 `EnableAlertJSONTail bool`

要求：

* 配置文件路径支持传入相对路径，但实际传给 Snort 时必须转换为绝对路径
* 若部分配置未传入，则从 `swd/wrap.db` 读取最近保存的配置
* `Mode=interface` 时必须提供网卡名
* `Mode=pcap` 时必须提供 pcap 文件路径

二、Snort 启动要求

* Snort 可执行文件必须严格使用 `${SNORT_DIR}/snort`
* `--daq-dir` 必须严格使用 `${DAQ_DIR}`
* 若对应文件或目录不存在，直接报错，不允许猜测默认路径
* `-c` 传入的配置文件路径必须是绝对路径
* 所有 `--lua` 覆写必须以独立参数方式传入，例如：

  * 正确：`--lua`, `ips = { ... }`
  * 错误：把整段 shell 字符串拼成一个参数
* 不要修改原始 snort.lua 文件内容，只通过命令行参数和生成文件实现覆写

三、规则管理
要求忽略原始配置文件中的 `ips.rules` 配置，统一改为使用 `swd/all.rules`

`all.rules` 生成方式：

* 使用 `swd/rules.db` 生成
* 若无 `rules.db`，则从 `swd/rules/*.rules` 初始化
* 初始化时默认启用全部规则
* 每次启动前重新生成 `all.rules`
* `all.rules` 仅包含 `enabled=1` 的规则
* 导出时直接使用 `raw_text`
* 保持稳定顺序

`rules.db` 至少包含表 `rules`，字段至少包括：

* `id INTEGER PRIMARY KEY AUTOINCREMENT`
* `sid INTEGER NOT NULL`
* `gid INTEGER NOT NULL DEFAULT 1`
* `rev INTEGER`
* `action TEXT`
* `proto TEXT`
* `src_net TEXT`
* `src_port TEXT`
* `direction TEXT`
* `dst_net TEXT`
* `dst_port TEXT`
* `msg TEXT`
* `classtype TEXT`
* `enabled INTEGER NOT NULL DEFAULT 1`
* `source_file TEXT`
* `raw_text TEXT NOT NULL`

规则解析要求：

* 不要求完整 AST
* 仅需提取基础字段：`action/proto/src_net/src_port/direction/dst_net/dst_port/msg/classtype/sid/rev`
* 其余内容保存在 `raw_text`
* 空行、注释需要跳过
* 若单条规则解析失败，记录错误并跳过，不要让整个初始化失败
* 如不支持跨行规则，请明确写在文档里；若支持更好

四、wrap.db
`wrap.db` 用于持久化最近一次有效运行配置。
可使用单表：

* `runtime_config(key TEXT PRIMARY KEY, value TEXT NOT NULL)`

至少保存：

* mode
* interface
* pcap_file
* swd
* snort_lua
* lua_overrides
* enable_alert_json_tail

五、Runner
根据 `Config` 初始化一个 `Runner`，至少提供：

* `Start() error`
* `Stop() error`
* `Restart() error`
* `Reset() error`
* `EnableRule(ruleID int64) error`
* `DisableRule(ruleID int64) error`

语义要求：

* `ruleID` 指 `rules` 表主键 `id`，不是 sid
* `Start()`：已运行时返回明确错误
* `Stop()`：未运行时返回可判定结果，不要 panic
* `Restart()`：等价于 `Stop()` + `Start()`
* `Reset()`：清空 `rules.db`，并在后续启动前重新从 `swd/rules/*.rules` 初始化
* `Stop()` 必须能停止 Snort 进程以及相关后台 tail goroutine
* 使用 `context` 管理进程和后台任务

六、alert_json tail + alerts.db
当启用 alert_json 文件输出时，tail `swd/alert_json.txt`，并将新增告警写入 `swd/alerts.db`

告警样例：
{ "timestamp" : "07/07-20:00:35.492196", "pkt_num" : 399, "proto" : "TCP", "pkt_gen" : "raw", "pkt_len" : 390, "dir" : "C2S", "src_ap" : "192.168.10.9:1035", "dst_ap" : "192.168.10.3:389", "rule" : "1:44604:1", "action" : "would_block" }

要求：

* 默认按每行一个 JSON 对象处理
* 只处理新增内容，不要求回溯全文
* 单行解析失败时记录错误并跳过
* `rule` 字段拆分为 `gid/sid/rev`
* Snort 重启或文件截断后应能继续跟踪

`alerts.db` 至少包含表 `alerts`：

* `id INTEGER PRIMARY KEY AUTOINCREMENT`
* `timestamp TEXT`
* `pkt_num INTEGER`
* `proto TEXT`
* `pkt_gen TEXT`
* `pkt_len INTEGER`
* `dir TEXT`
* `src_ap TEXT`
* `dst_ap TEXT`
* `gid INTEGER`
* `sid INTEGER`
* `rev INTEGER`
* `rule TEXT`
* `action TEXT`
* `raw_json TEXT NOT NULL`
* `created_at TEXT NOT NULL`

七、CLI
`wrap2/cmd` 至少提供这些子命令：

* `start`
* `stop`
* `restart`
* `reset`
* `enable-rule <id>`
* `disable-rule <id>`
* 建议增加 `status`

CLI 仅作为对 module 的薄封装，不要把核心逻辑写进 cmd

建议支持参数：

* `--swd`
* `--mode`
* `--iface`
* `--pcap`
* `--config`
* `--lua`（允许重复传入）

八、文档
在 `wrap2/docs` 中写简略文档，至少包括：

* 包结构说明
* `Config` 字段说明
* `Runner` 接口说明
* 一个 module 调用示例
* 一个 CLI 调用示例
* 已知限制和假设

九、测试环境
测试用环境变量：

* `SNORT_DIR=/home/c/snort-optimizer/wrap/snort/build/src`
* `DAQ_DIR=/home/c/snort-optimizer/wrap/snort/libdaq/build/lib/daq`

测试配置文件：

* `/home/c/snort-optimizer/wrap/config/snort.lua`

十、验收标准
完成后请确保：

1. `go build ./...` 可以通过
2. CLI 可以编译
3. 首次运行无 `rules.db` 时可从 `swd/rules/*.rules` 初始化
4. 启用/禁用规则后可重新生成 `all.rules`
5. 能正确拼接 Snort 启动命令
6. `alert_json.txt` 新增一行 JSON 后可入库 `alerts.db`
7. 输出目录树
8. 说明你做了哪些设计假设

如果你发现需求存在关键歧义，请停止编码并先列出问题。
