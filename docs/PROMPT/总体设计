这是我的毕设文件夹，我的毕设项目是snort性能优化器，会通过优化snort的配置文件和裁剪snort规则来优化snort的性能。
wrap是snort的go wrap。
analyser是snort的规则分析文件夹，用于根据snort给出的profiler裁剪规则
web是前端页面文件夹。

现在wrap既包括wrap，也包括后端。你的任务是参照wrap文件夹下的内容（也可以不参照，只要功能实现了），在wrap2文件下下写一个功能更纯粹的wrap module。

要求：
1.支持cmd和module调用，cmd相关的逻辑和主函数写到wrap2/cmd文件夹下
2.传入一个config结构来配置snort的运行，里面要包含：
	（1）snort的运行模式：监听网卡或者读取pcap文件
	（2）snort的工作目录（以下简称swd）
	（3）snort的配置文件位置（支持传入相对路径，但是传给snort的时候使用绝对路径来保证兼容性）
	若未传入，直接使用swd下wrap.db保存的配置
	wrap.db保存的
	（4）支持使用--lua进行snort的配置覆写

3.要求本wrap直接覆写rules配置：忽略配置文件中的ips.rules配置，改为使用swd中的all.rules(all.rules生成规则如下)
	使用swd下的rules.db生成all.rules。若无rules.db，则根据swd下的rules文件夹中的*.rules初始化rules.db，默认启用所有规则
	初始化rules.db时使用的字段包括rules的基本字段和enabled字段
	rules参考：
	alert tcp $EXTERNAL_NET any -> $HOME_NET 389 ( msg:"SERVER-OTHER Novell eDirectory LDAP server buffer overflow attempt"; flow:to_server,established; content:"|30 84|",depth 2; byte_test:4,>,0xFFFFFF,0,relative,little; metadata:policy max-detect-ips drop; service:ldap; reference:url,download.novell.com/Download?buildid=bXPFv5btgsA; classtype:attempted-user; sid:44604; rev:1; )

4.根据config初始化一个runner，然后runner要支持以下操作：
	（1）reset() 即直接清空rules.db。
	（2）disable_rule(rule_id) 操作数据库
	（3）enable_rule(rule_id) 启用规则
	（4）restart() 重启snort
	（5）start() 启动snort
	（5）stop() 停止snort

5.把支持的接口调用写入wrap2/docs文件夹，简略说明即可

6.如果启用了alert_json，file = true选项，就tail alert_json.txt，然后在swd下使用sqlite3进行入库alerts.db，若不存在则初始化数据库，警告参考如下：
{ "timestamp" : "07/07-20:00:35.492196", "pkt_num" : 399, "proto" : "TCP", "pkt_gen" : "raw", "pkt_len" : 390, "dir" : "C2S", "src_ap" : "192.168.10.9:1035", "dst_ap" : "192.168.10.3:389", "rule" : "1:44604:1", "action" : "would_block" }

7.把rules, alert这种比较通用的类型定义直接存储在types文件夹作为module再引用
8.运行snort的命令中，snort程序的地址和--daq-dir严格按照环境变量SNORT_DIR和DAQ_DIR执行，找不到文件直接报错。
9.测试用信息：
	SNORT_DIR /home/c/snort-optimizer/wrap/snort/build/src
	DAQ_DIR /home/c/snort-optimizer/wrap/snort/libdaq/build/lib/daq
	snort config位置 /home/c/snort-optimizer/wrap/config/snort.lua

10. 不要把很多东西都放在一个go文件里，适当分package。

11.有不确定的内容请暂停并询问我
