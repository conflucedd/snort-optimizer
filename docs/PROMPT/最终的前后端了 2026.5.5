对实现的后端（server）和前端（web）进行改进
1.后端启动snort的时候调用wrap好像忘记了need-alert，导致没有任何警报
2.后端的cmd选项改为“--”而不是“-”
3.前端排版改进：AWD设置应该放到“规则优化”界面
4.“概览”界面没有实现抓包功能：即抓取当前真实的业务流量并保存以供后续分析
5.配置优化的interface设置应该改为下拉选择
6.配置优化的run_id不应该被设置，生产环境应当默认是0
7.保存按钮不弹出信息框，检测到修改才亮起，否则变灰
8.后端文件夹是server，但是里面还有一个文件夹是server，不太清晰，这个可以改吗
9.现在的各种折线图很难看，可以尝试使用专用的绘图库，要求横坐标每个点的距离要相等，没启动时不绘制，可以参考windows 10的任务管理器cpu占用绘制的逻辑。
10.“规则优化”页面里有一个“恢复”按钮，但是我的analyser好像没做恢复逻辑，请去掉这个恢复，如果没跑完，只需显示跑了一半的结果即可，但是不能继续跑。
11.前端页面直接嵌入后端，去掉选择前端的选项
12.优化文件夹安排逻辑，后端的各种默认路径都设成相对后端work dir的路径（假设server在snort-optimizer的根目录跑），比如配置就是config/snort.lua，抓包的真实流量存放在real_pcap。然后snort运行目录就默认是SWD，分析目录默认是AWD。（同时支持绝对路径）。
13.有不明白的可以暂停问我。参考的analyser运行结果在/tmp/test_anb。