为这个snort-optimiser写一个前端和后端。
说明：项目分成4个部分：wrap（已完成，snort的go wrap），analyser（已完成，规则分析器），server（未做，后端，提供http访问和控制等），web（未做，前端项目文件夹）。可以参考的文档在docs文件夹（docs/PROMPT文件夹除外）。

要求：
1.前端的设计思路是左边一个小侧栏，控制要显示的页面。页面分为：
    （1）概览：显示当前snort实例的cpu，mem，抓包状态，系统的连接数和各种统计等。cpu和mem要有折线图。性能测试功能（用来测试调优结果，注意和analyser进行区分， 那个是规则的性能优化，这个是总体的性能测试）；简单的控制功能：重启，停止，重置等。（对于重启按钮，如果用户改完配置要变颜色，提醒用户重启）。
    （2）警告页面：显示snort的警告。（注意可能规模很大，示例的snort.sqlte在/tmp/test_anb/exp/snort.sqlite）
    （3）规则优化：最重要的页面，主要是analyser模块的前端。先选择需要分析的流量等，然后进行分析，analyser输出好的结果的示例在/tmp/test_anb。请为那个规模的数据做出合理的操作和折线图（比如多轮的误报率，漏报率变化，吞吐率变化（注意不是cpu变化，因为分析pcap肯定会跑满单核）），还有进度，提交/Rollback显示等（可通过查数据库估算进度）。注意run_id。注意analyser跑的时间很长（可能达到2h），所以如果发现跑完过一次分析，检测到数据库有东西就要还原原来的界面，直到手动开新分析实例。注意为了保证性能分析精确，如果用户覆写了--lua，那么传给analyser的--lua也要相同。（后端持久化）
    （4）配置优化：主要是使用--lua的覆写。比如实现开关模块等，需要加一些常用的--lua覆写参数，并作出简要说明。还有手动开关规则（直接操作snort.sqlite数据库），需要根据analyser的结果智能显示和建议。
    （5）系统优化：主要是网卡硬件offload开关，snort的cpu亲和性等。（只控制生产环境，可能需要申请root）
2.后端需要调用wrap，analyser，sql这些package。但是还要实现一些新的功能（主要是配置优化和系统优化这两个界面，规模比较小）。需要有一个数据库存储各种开关配置（比如用户手动设置的lua覆写开关，手动启用或者禁用的规则）等。

3.后端的文件夹安排建议为
    /server.go
    /pcap 存放抓包的文件，用于给analyser传入真实的业务流量。
    /SWD 生产环境的snort工作目录
    /AWD analyser的工作目录
    /server.db server的配置持久化
    /sql 数据库相关
    /config-optimize 负责snort的配置文件优化
    /system-optimize 
    ... 可以自己添加，也可以修改上面的，只要命名更合理

4.前端要使用react框架，整体以白色为底，要有动画但是动画要迅速和跟手整体可以参考cloudflare Dashboard的样式。

5.不要生成很大的文件，适时拆成文件夹和小文件。

6.types package包含了公共的类型定义，请尊重这些定义。

有什么不明白的或者更好的建议请暂停问我。